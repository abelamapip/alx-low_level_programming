#include "main.h"
/**
 * main - check the code
 * print_alphabet - prints lowercase alphabet
 * Return: Always 0.
 */
int main(void)
{
print_alphabet();
return (0);
}

void print_alphabet(void)
{
int a = 97;
while (a < 123)
putchar(a);
putchar(10);
}
